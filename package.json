{
  "name": "digitalodyssey",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd ./backend && npm i && cd ../frontend && npm i && npm run build && cd .. && cp -r frontend/dist backend/",
    "start": "cd ./backend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "digitalodyssey": "file:"
  }
}
